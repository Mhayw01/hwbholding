[build]
  # Static site. The Netlify dashboard has a stale "Publish directory: build"
  # from the old React app that overrides at the deploy stage, so we assemble a
  # build/ directory containing the site and publish that. No real build needed.
  command = "rm -rf build && mkdir build && cp -r index.html assets build/"
  publish = "build"
